# Input: N (number of problems), X (marks per problem), Y (desired marks)
N, X, Y = map(int, input().split())

# Check if Y is a multiple of X and if Y // X is less than or equal to N
if Y % X == 0 and Y // X <= N:
    print("YES")
else:
    print("NO")
